import { ChevronLeft } from "lucide-react";
import { Link } from "react-router-dom";
import { CATEGORIES } from "../../../lib/data/products";
export default function CategoryHero({ category, id, total, loading }) {
  return <section className={`relative overflow-hidden ${category?.bg || "bg-forest"}`} aria-labelledby="category-title"><div className="max-w-7xl mx-auto px-6 py-14"><div className="flex items-center gap-2 text-sm mb-6 text-cream/70"><Link to="/" className="hover:underline hover:text-cream">خانه</Link><ChevronLeft size={14} aria-hidden="true" /><Link to="/#categories" className="hover:underline hover:text-cream">دسته‌بندی‌ها</Link><ChevronLeft size={14} aria-hidden="true" /><span className="text-cream" aria-current="page">{category?.name}</span></div><div className="flex items-end justify-between flex-wrap gap-4"><div><h1 id="category-title" className="text-3xl md:text-4xl font-extrabold text-cream mb-3"><span className="me-2" aria-hidden="true">{category?.emoji}</span>{category?.name}</h1><p className="text-cream/70" aria-live="polite">{loading ? "در حال بارگذاری..." : `${total.toLocaleString("fa-IR")} محصول در این دسته‌بندی`}</p></div><nav aria-label="دسته‌بندی محصولات" className="flex flex-wrap gap-2">{CATEGORIES.map((current) => <Link key={current.id} to={`/category/${current.id}`} aria-current={current.id === id ? "page" : undefined} className={`text-sm font-semibold px-4 py-2 rounded-full border transition-colors ${current.id === id ? "bg-cream text-forest border-cream" : "border-cream/30 text-cream hover:bg-cream/10"}`}>{current.emoji} {current.name}</Link>)}</nav></div></div></section>;
}
