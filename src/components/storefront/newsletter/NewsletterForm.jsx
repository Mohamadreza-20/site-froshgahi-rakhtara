import { CheckCircle2, Mail, Send } from "lucide-react";
export default function NewsletterForm({ email, error, subscribed, onChange, onSubmit }) {
  return <form noValidate onSubmit={onSubmit} className="flex flex-col sm:flex-row gap-3 max-w-md mx-auto items-start"><div className="w-full flex-1"><div className="relative"><Mail size={18} aria-hidden="true" className="absolute right-4 top-1/2 -translate-y-1/2 text-ink/35 pointer-events-none" /><input id="newsletter-email" type="email" inputMode="email" autoComplete="email" value={email} onChange={onChange} placeholder="ایمیل شما" aria-label="ایمیل برای عضویت در خبرنامه" aria-invalid={Boolean(error)} aria-describedby={error ? "newsletter-error" : undefined} className={`w-full bg-white pr-11 pl-5 py-3 rounded-full text-ink placeholder:text-ink/40 outline-none ring-2 transition-shadow ${error ? "ring-red-400 focus:ring-red-400" : "ring-transparent focus:ring-camel"}`} /></div>{error && <p id="newsletter-error" role="alert" className="text-xs text-white bg-red-500/25 rounded-lg px-3 py-1.5 mt-2 text-right">{error}</p>}</div><button type="submit" className="cursor-pointer shrink-0 flex items-center justify-center gap-2 bg-camel hover:bg-camel-light text-forest font-bold px-6 py-3 rounded-full transition-all hover:scale-[1.02] glow-camel" aria-live="polite">{subscribed ? <CheckCircle2 size={18} aria-hidden="true" /> : <Send size={18} aria-hidden="true" />}عضویت</button></form>;
}
